import { TUserInfoProps } from "../../components/UserInfo/UserInfo";

export type TArticlePageProps = {
  bannerProps: Banner;
  userInfoProps: TUserInfoProps;
  followButtonProps;
  favoriteButtonProps;
  commentBoxProps;
};
